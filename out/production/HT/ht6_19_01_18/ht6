Субботняя задача:
-Создать класс Student  с полями name, age.
-Создать класс ClassRoom с полями  String classWord; int classLimit; Student[] studMass;

Необходимо создать 40 студентов, у каждого из которых должен быть указан возраст(произвольный в диапазоне от 7 до 16 лет,
randomNum = ThreadLocalRandom.current().nextInt(min, max + 1) -вам в помощь) и имя. После чего нужно создать класс А и класс Б.
В класс А записать(добавить студентов) возрастом от 7 до 12 лет, остальных в класс Б.
При чем, каждый класс имеет макс кол-во студентов. Для А -12, для Б -15.
Собственно для всех мест не хватит,
-После добавления мы должны видеть сколько студентов не смогло записаться в класс и какие именно.
- Реализовать методы:
  показывающие сколько человек есть в в конкретном классе
  показывающей всех студентов класса(их имена и возраст)
  показывающей всех студентов класса(только имя)
  показывающий всех   в отсортированном порядке по алфавиту
                                                по возрасту
                                                и наоборот(реверс) по обоим полям.
 метод который находит конкретного студента по имени в конкретном классе
 метод который ищет и показывает в каких классах есть студент с таким именем
            если их несколько( возможно нужно будет добавить дополнительное поле в один из классов)
* доп задание - когда мы создаем студентов, создать метод который будет писать произвольные - нормальные имена

1. Реализовать следующие доработки задачи студенты в школе. Базовое условие ht_12)01_18.

Добавить в класс Student следующие поля:
-адрес
-город
-область
-телефон

Реализовать логику, с помощью которой мы сможем получить:
- все выше указанные данные
- только запрашиваемый атрибут.

- Реализовать в соответствии с рекомендациями 4-й главы по Хортсману.

Создать класс School, который содержит поля:
- номер школы
- адрес
-телефон приемной
- Список классов в школе.

Реализовать следующую логику:
- Когда создаем объект школа, по умолчанию она должна иметь 2 класса
"А", "Б" - украинские классы, "Д","З" - английские.
- добавить соответствующее поле классу ClassRoom.

При чем, если я создаю объект школа, должна быть возможность указать количество, и буквы
классов в школе. Если же ничего не указывать, то должна срабатывать логика по умолчанию.

Реализовать логику добавления новых классов, в дополнение к имеющимся.

У школы должны быть методы:

показывающий сколько и какие классы есть в школе.
сколько украино язычных, сколько англоязычных.
сколько студентов в каждом классе
сколько всего студентов учится в школе.
сколько и в каком классе.  ???????
какой возрастной диапазон.

Добавить в школу еще 30 студентов за признаком языка, исходя из того, что вместимость новых классов
не более 15 человек.
Возможно Student будет нуждаться в дополнительном поле.

2. Читаем по Хортсману глава 5. - только под главу Глобальный супер класс Object